<% @page_title = "会員管理" %>
<h1><%= @page_title %></h1>

<% if @customers.present? %>
  <table class="list">
    <thead>
      <tr>
        <th>背番号</th>
        <th>ユーザー名</th>
        <th>氏名</th>
      </tr>
    </thead>
    <tbody>
      <% @customers.each do |customer| %>
        <tr>
          <td style="text-align: right"><%= customer.id %></td>
          <td><%= link_to customer.user_name, customer %></td>
          <td><%= customer.full_name %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>会員情報がありません。</p>
<% end %>